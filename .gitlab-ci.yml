include:
  - remote: https://gitlab.com/msclock/gitlab-ci-templates/-/raw/v2.5.46/collections/All-Projects.gitlab-ci.yml
  - remote: https://gitlab.com/msclock/gitlab-ci-templates/-/raw/v2.5.46/jobs/Pages-Mkdocs.gitlab-ci.yml
  - remote: https://gitlab.com/msclock/gitlab-ci-templates/-/raw/v2.5.46/.gitlab/includes/Detect-Changes-Build-Images.gitlab-ci.yml

variables:
  LINT_YAML_ENABLE: ''
  LINT_MD_ENABLE: ''
  # Post to run tag pipeline to trigger released pipeline
  RELEASE_POST_PIPELINE_TYPE: tag

# @Description lint codebase with pre-commit
pre_commit:
  stage: lint
  before_script:
    - |
      git config --global url.https://mirror.ghproxy.com/https://github.com.insteadOf https://github.com

# @Description count codes
code_count:
  before_script:
    - |
      sed -i 's/dl-cdn.alpinelinux.org/mirrors.tuna.tsinghua.edu.cn/g' /etc/apk/repositories

._pages_common: &pages_common
  variables:
    VERSION_DOCS: '1'
    MKDOCS_EXTRA_PLUGINS: mkdocs-material mkdocs-git-revision-date-localized-plugin mkdocs-same-dir
  before_script:
    - |
      pip config set global.index-url https://mirrors.tuna.tsinghua.edu.cn/pypi/web/simple
      pip config set global.trusted-host mirrors.tuna.tsinghua.edu.cn

# @Description review deploy pages with mkdocs
pages:review:
  <<: *pages_common

# @Description deploy with mkdocs
pages:
  <<: *pages_common

# @Description semantic_release
semantic_release:
  variables:
    NPM_SOURCE: https://registry.npm.taobao.org
    RELEASE_EXTRA_PLUGINS: >-
      @semantic-release/changelog
      @semantic-release/git
      semantic-release-replace-plugin
      conventional-changelog-conventionalcommits
      @semantic-release/exec
